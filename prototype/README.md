# DiaperScout prototype

An HTML-first workspace for exploring the DiaperScout experience.

Open `index.html` in a browser to launch the prototype. Shared design language lives in `css/coastal-explorer.css`; every screen in `screens/` links back to the launcher.

## Current focus

`screens/product.html` is the next screen for detailed design and interaction work.
